name: examspark_frontend
description: "A new Flutter project."
publish_to: 'none'

# Version update karna zaroori tha Play Console ke liye (2 se 3 kar diya hai)
version: 1.0.0+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.0
  firebase_core: ^4.12.1
  google_fonts: ^6.2.1
  url_launcher: ^6.3.1
  receive_sharing_intent: 1.7.0
  posthog_flutter: ^5.24.0
  # Play Console Subscriptions ke liye library add ho gayi hai 👇
  in_app_purchase: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

